# Causal Compression

We're trying to extend the causal abstraction framework of Kinney & Lombrozo (2024) from single-agent representation to speaker-listener communication. 

## Installation

```bash
pip install -e .
```

See `notebooks/demonstrations.qmd` for worked examples.

